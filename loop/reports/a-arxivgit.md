# a-arxivgit — progress report

## 1. claim
arXiv git-frontier swept (GCC, GitOfThoughts, STORM, Ledger, OpenWeft,
self-evolving + memory waves); S24–S34 steals banked; CONTEXT command live.

## 2. evidence
- 3 deep sweeps; GITNATIVE §0 with mechanisms + numbers (GCC 80.2%,
  STORM +18.7, Ledger +8pp/-29%, RHO 59→78%).
- `loop.py context` L0–L3 hierarchical retrieval (packet → ready/paused →
  summaries → full records); nesting tested (L0 excludes ready, L3 dumps).
- Honest boundaries recorded (GitOfThoughts τ≈0.8 copyability limit;
  memory helps retrieval-reuse, git's case is auditability).
- Steals S24–S34 specified with status (1 implemented, rest queued with
  homes: notes/scores, keyed layout, invalidation, intent fields, scheduler,
  pre-reg commits, edit contracts, consolidation, rollback, heuristics).

## 3. self-review
Sweep summaries, not full-paper reads (highlights + abstracts; theorems not
verified). Steal specs untested by construction (next turns implement).
CONTEXT L2 lists summaries but not report bodies (depth tuning open).

## 4. needs
S-steal implementations (ready backlog). Queues otherwise unchanged.

## 5. cost
$0 (searches + docs + local code), no manual actions.
