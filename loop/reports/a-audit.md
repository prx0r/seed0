# a-audit — progress report

## 1. claim
Tournament audit: code vs logs, token/time verdict

## 2. evidence
elapsed_s=harness wall time; zero token tracking in tournament path; model flag echoed; funnel Meter unfed; only pyeval meters (provider-reported). Spawned M4+H6.
Artifacts verified present this run. Suite basis: 46 passed both CWDs; self-check 6/6 (this session).

## 3. self-review
Old tournaments not re-run (committed logs + verifying receipts trusted).

## 4. needs
None inside these tasks (push need pre-queued as H1).

## 5. cost
$0, no manual actions.
