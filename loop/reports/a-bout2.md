# a-bout2 — progress report

## 1. claim
Bout2 demand-side closed: de-primed brief shifted the market (EICR→boilers),
3/3 lanes exit 0, blind review, receipted.

## 2. evidence
- Frozen: brief (no domain examples) + rubric + validator + weights.
  Baseline exit 1 calibrated.
- Lanes: gas-boiler servicing ×3 (districts/turnover/Leicester framings),
  9/8/6 tests, all exit 0 first try; independent re-run 3/3.
- score_attempt: 3/3 binary_pass, 5/5 compliance. Blind review (3 lanes,
  all promote, lane-visible fields only) → revealed → tournament receipted
  (sha256_5a2c…): seed3 #1 (evidence 3), seed5 #2 (2), seed1 #3 (1) —
  evidence-luck ordering, third occurrence, now a standing pattern.
- De-prime verdict: PARTIAL success. Withheld EICR examples → zero EICR
  builds, but "compliance/service trade" primed boilers 3/3. Next: withhold
  domain hints entirely (pure mechanism brief).

## 3. self-review
Demand estimators use hardcoded mock data (estimates labeled as such per
brief — honest, but unvalidated against any real housing numbers; H-sparky
pilot or oracle data would ground one).

## 4. needs
Nothing new (H-sparky covers world-validation).

## 5. cost
$0 (3 subagent lanes inside session compute), no manual actions.
