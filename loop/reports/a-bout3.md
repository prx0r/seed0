# a-bout3 — progress report

## 1. claim
Repeat with changes closed: pure-mechanism brief killed domain convergence
(mechanism family remains — correctly), 3/3 exit 0, receipted.

## 2. evidence
- Changes from bout2 review: (1) zero domain language in brief (no trade,
  market, compliance words); (2) log-then-light ordering rule banked in
  TOURNAMENTS runbook.
- Lanes: linear-weighted formulas (2a+b-c / 2α+β-γ / 2x+3y-z), distinct keys,
  graceful coercion, round-trips; 9/8/6 tests; all exit 0 (one try-2);
  independent re-run 3/3.
- score_attempt: 3/3 binary_pass, 5/5. Blind review (A/B/C) → reveal →
  tournament receipted (sha256_3286…): evidence-luck order ×4 (standing).
- Convergence analysis: domain gone, mechanism family remains (linear
  scoring is THE natural answer to "rank by formula" — convergent evolution,
  not priming; the distinction matters and is now documented).

## 3. self-review
Formulas unvalidated against any real ranking problem (toy domain by design).
Weights (2,1,-1 etc.) unjustified — lanes chose, none defended. Next: bout
with a REAL ranking dataset (fixed gold, measured footrule — meta-tourney
pattern applied to lanes).

## 4. needs
Nothing new.

## 5. cost
$0 (3 subagent lanes inside session compute), no manual actions.
