# a-daylog — progress report

## 1. claim
Day-recording evaluated + tried: headless box can't record; user laptop can
(ScreenPipe/ActivityWatch); idea banked as bout-ready idea3.

## 2. evidence
- Box: DISPLAY empty, no login session, ffmpeg x11grab fails ("Cannot open
  display :0") — capture proven vacuous here, not asserted.
- Research: ScreenPipe (source-available, Linux CLI ok, MCP server, REST
  :3030, ~300MB/8hr event-driven vs ~2GB continuous; Rewind dead since
  Dec 2025) vs ActivityWatch (free/MPL, REST+buckets+query API, aw-client,
  editor plugins; needs desktop session for window watcher).
- File size answered: event-driven screenshots+JPEG ≈300MB/8hr, not huge.
- `ideas/idea3-neuroaudio.md`: IDEA OK [0ms]; science labeled honestly
  (state-dependent memory real-modest; binaural transfer unproven).
- A-framework: this work ran as A-task (reads+probes+$0); H remainder below.

## 3. self-review
No software installed or executed beyond probes (disk 93% respected).
ScreenPipe recommendation untested by hand (needs a desktop; stated).

## 4. needs
H: install ScreenPipe (or ActivityWatch) on YOUR laptop, point its MCP/API
at an agent, approve data sharing per app. M: ScreenPipe paid tiers only if
wanted (free/local core suffices). Agent-side: our loop/decisions/spans
JSONL already IS the machine-readable work record — the missing half was
always the human side.

## 5. cost
$0, no manual actions.
