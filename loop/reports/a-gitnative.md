# a-gitnative — progress report

## 1. claim
Git-native turn: scaffold git-inits, goal tree renders, frontier mined for
steals (2 adopted, 5 queued).

## 2. evidence
- `seed0.py new` git-inits with local identity + scaffold commit (test:
  `.git` exists, log shows scaffold, email is seed0@local).
- `loop.py map` renders live queue as DAG tree (test: ordering + branch
  annotation + orphan surfacing); ran on live queue this turn.
- Frontier: Letta MemFS/Context Repos, ait-vcs, git-lanes, aide-memory,
  Hermes worktrees, Aider (2 deep searches). workerun: searched, absent
  (closest: WorkerKit/RunWorktree/HarnessRun — awaiting one-word correction).
- `docs/GITNATIVE.md`: patterns, estate telemetry+bus review, S17–S23 steals.

## 3. self-review
429s blocked the live metered run (documented, not retried into a wall).
P4/P5 tests (metrics/leafcheck/replan already implemented) deferred to keep
this turn on the git directive — queued explicitly, not dropped.

## 4. needs
H-queues + M4 unchanged. Backlog +S19–S23.

## 5. cost
$0 (429s: $0.00 spent), no manual actions.
