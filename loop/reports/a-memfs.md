# a-memfs — progress report

## 1. claim
MemFS reviewed at source, SKILL vendored, M-failsafe stack specced, one live
credential-hygiene finding banked.

## 2. evidence
- Letta paths in cloned HEAD don't exist (web snippets = letta-code repo /
  newer tree) — stated, not fudged; SKILL.md fetched from canonical raw URL
  and vendored with Apache-2.0 provenance header.
- MemFS model reviewed: git-projected memory, harness commits/pushes,
  worktree concurrency, frontmatter hooks, backup remotes, conflict protocol.
- WorkerKit .mds: HANDOVER (marketplace registry + vault), SCORING formula
  (agentability×4 + revenue×3 + api×2 + ease×1 — usable priority math),
  Moltwork protocol objects (Mandate→…→Receipt, pydantic, hashed minus
  volatile). providers/ confirmed GONE from master (history-only).
- Harbor answer: NOT bundles — immutable task snapshots (source_commit +
  rubric hash + hidden ref) + trajectory + manifest + regrade-without-rerun
  + WorkerKit binding. Bundles remain OUR transport (verified separately).
- Canonical pick: Letta memory-git + Harbor snapshots + Moltwork money
  objects + seed0 loop/tournaments (each cited with file).
- M-failsafe L0–L7 in REGISTRIES §8 (each layer fail-closed + tested).
- Credential finding: mw HANDOVER (public repo, pushed) holds vault master
  password in FULL (keys truncated — safe) and vault DB is NOT tracked
  (only oracle.db market data). Severity: rotate password + purge file when
  convenient; no live keys exposed. Queued as H (owner-only action).

## 3. self-review
SKILL vendored by transcription (fetched content re-typed with header, not
byte-copied — content verified section-by-section against fetch). Harbor
conclusion from PLAN + dir listing, not a live trial (stated). Scoring
formula noted, not adopted (needs revenue calibration first).

## 4. needs
H-credrotate (mw vault password) — owner only. Nothing else new.

## 5. cost
$0, no manual actions.
