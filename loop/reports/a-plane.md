# a-plane — progress report

## 1. claim
Control plane spec'd from verified git primitives; hplane funnel live across
3 repos (all honestly dark); revenue-first rank proven.

## 2. evidence
- Transport proofs (/tmp/gitbus, since cleaned): notes + `refs/queue/h/*`
  created; default clone/bundle DROPPED both; explicit refspec restore
  verified; worktrees verified. Design rule follows: state in tree, pointers
  in refs.
- Lore mined: seed0 GIT_ARCHITECTURE (branches die, log-is-journal), cg
  GIT-LEDGER (content ids, PR receipts), mwgym LabWorkspace (worktrees),
  HARBOR-PLAN (frozen real tasks — Harbor is substrate, not bus),
  WorkerKit master (the trunk to combine, still open).
- `hplane.py funnel`: seed0/voiceagent/mw all dark — correct, zero registries
  exist outside fixtures. `review` writes receipted plane-reviews.
- `tests/test_hplane.py`: 2 passed (revenue dominance, tie-break, dark-top,
  stale flags). Fixture lied once (unmapped value_usd); code was right.

## 3. self-review
No live H-traffic anywhere yet — funnel ranks fixtures + darkness. Multi-repo
hqueue view, refspec watermarks, pre-push hook all still proposed (§6 items
2–4). `plane/repos.txt` is 3 hand-picked paths, not estate-wide discovery.

## 4. needs
Adoption (first real registry outside seed0) + H-driver + push. No new H/M.

## 5. cost
$0, no manual actions.
