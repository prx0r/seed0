# a-primdoc — progress report

## 1. claim
Canonical A-catalog banked as PRIMITIVES Part II (33 things, one function each).

## 2. evidence
`docs/PRIMITIVES.md` Part I (10 bout laws, untouched) + Part II (7 groups:
harness 13, delegation 3, loop 5, streams 4, validators+seeds, state files).
Every entry: file, function, in/out, tier, proof test. Seed one-liners match
README kernel names; seed1 notes 1.1 delta.

## 3. self-review
Catalog is descriptive (written from the code as-built this session), not
independently audited line-by-line — a stale entry is possible where code
moved (mitigation: index-links-live + suite green; catalog has no executable
checks — proposed B12: catalog consistency test).

## 4. needs
None. Queues unchanged.

## 5. cost
$0, no manual actions.
