# a-reviewip — progress report

## 1. claim
ip-graph reviewed (functions, skills, visionary docs, experiments); 18 file
links verified; steal list S1–S6 banked as ready backlog.

## 2. evidence
`/tmp/opencode/ip-graph` refreshed on disk (8.4M export, no .git — stated).
SKILL.md read whole (command map, honest rules, RAM block). MASTER layers,
ORCHESTRATION loop, HERMES-API, pipeline_verify A/B/C, experiment_lab +
5-work proof log, CANONICAL-DAG, MANIFEST+check, NAVIGATION, VISION doc read.
15 SKILL.mds swept box-wide for the visions memory: no endpoint-decomposition
skill found (honest negative + one unchecked candidate named, not chased).
All 18 review links existence-checked (1 miss caught + fixed pre-bank).

## 3. self-review
Read-only review (nothing executed — heavy deps unverified, stated). Steal
items are proposals with file pointers, not ports (ports = next tasks).
Visions answer could be wrong if the skill lives outside swept paths.

## 4. needs
Steal ports S1–S6 (ready A-backlog). Nothing H/M.

## 5. cost
$0 (8.4M disk, 5.7G free remain), no manual actions.
