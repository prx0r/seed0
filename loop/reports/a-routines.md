# a-routines — progress report

## 1. claim
cg-grade routines live: core-loop diagram in GUIDE, R1–R6 with exact commands,
WHEN matrix, 4 workflow combinations.

## 2. evidence
`docs/ROUTINES.md`: core loop diagram (matches funnel+loop+receipts reality);
R1 (bring-up, 71-test expectation re-verified this session), R2 (bout, points
at meta-tourney worked example), R3 (chain1 worked example), R4 (release
rules), R5 (peer-review pattern), R6 (BOOT short form); WHEN matrix (8 rows);
combinations (full chain, driver pulse, estate sweep, incident). GUIDE top
gained the same diagram + pointers. Commands quoted are ones run this session
(globs like seeds/seed* are shell-standard).

## 3. self-review
Expected outputs (71 tests) rot as suite grows — same staleness class as any
doc with numbers (mitigation: checker + suite are the live truth; docs point
at them). R4 push transport assumes credential present (H1b covers).

## 4. needs
None. Queues unchanged.

## 5. cost
$0, no manual actions.
