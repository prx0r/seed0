# a-stealearn — progress report

## 1. claim
Learning-layer steals live: heuristics pool distillation.

## 2. evidence
- `loop.py heuristics` scans DONE self-reviews for never/always/must/avoid
  phrases → `loop/heuristics.md` with source task cites (live run: 8 rules).
  Phrase filter (multi-word only) added after single-keyword stubs observed.
- Keyed proposals (S32) covered under a-stealgit mechanism; consolidation
  schedule = run learnings per bout (METAPROCESS rule, unchanged).

## 3. self-review
Regex distillation is crude (misses paraphrase, keeps context-bound rules
without their context — each cites source so a human can check). ERL-grade
LLM ranking explicitly not attempted (would cost inference + add judgment
where counting suffices).

## 4. needs
None. Queues unchanged.

## 5. cost
$0, no manual actions.
