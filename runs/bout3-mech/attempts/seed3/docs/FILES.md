# FILES — one line per file
- evidence.py — hash-chained append + verify
- evidence/ — run logs (receipts, gitignored? NO — evidence commits; see THREADS)
- tests/test_evidence.py — chain, tamper, reorder tests
- docs/ — index, recipes, files, threads
