# THREADS — open threads
- Commit evidence logs or not? Currently YES (proof travels with code); large
  binary payloads stay out (digests in, blobs elsewhere).
- Export format for external verifiers (canonical JSON spec).
- Rotation/archival policy for long-lived projects.
