# seed6-ham

HAM registry tournament seed: auditable autonomy via task lifecycle plus locked money plus unlock graph

## 60-second start

```bash
cp .env.example .env   # fill keys; never commit .env
[install: pip install -r requirements.txt]
[run: ./scripts/run.sh or equivalent]
[verify: pytest tests/ -q + scenario script]
```

## What this is / is not

- IS: [one paragraph — the job this repo does]
- IS NOT: [ neighbouring systems' lanes — name them, link them ]

## Layout

[One line per top-level dir. Keep FILES.md authoritative.]
